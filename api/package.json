{
  "name": "@seeds4bees/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Lambda backend for seeds4bees.net",
  "scripts": {
    "test": "node --test"
  },
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.679.0",
    "@aws-sdk/lib-dynamodb": "^3.679.0"
  }
}
